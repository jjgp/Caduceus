#!/usr/bin/env bash

set -euo pipefail

# curl -L https://api.github.com/repos/zoom/zoom-sdk-ios/zipball/master
# curl -s https://api.github.com/repos/zoom/zoom-sdk-ios/releases/latest | grep browser_download_url | awk -F\" '{print $4}' i
# curl -s https://api.github.com/repos/zoom/zoom-sdk-ios/git/refs/tags | grep \"ref\" | tail -1

